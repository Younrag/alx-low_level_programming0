#include "main.h"
/**
 * _isupper - Checks for uppercase character
 * @c: caracter
 * Return: 1 if c is uppercase
 */

int _isupper(int c)
{
if (c >= 'A' && c <= 'Z')
{
return (1);
}
else
{
return (0);
}
}

